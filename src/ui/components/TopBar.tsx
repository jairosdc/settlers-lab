import type { GameState } from '../../engine/types'
export function TopBar({ state, onPause, onSpeed, onSave, onLoad, onReset, onExport, onImport }: { state: GameState; onPause:()=>void; onSpeed:(s:1|2|4)=>void; onSave:()=>void; onLoad:()=>void; onReset:()=>void; onExport:()=>void; onImport:(file:File)=>void }) {
 return <header className="topbar"><strong>Settlers Lab</strong><span>Tiempo {Math.floor(state.time/60)}m {Math.floor(state.time%60)}s</span><button onClick={onPause}>{state.paused?'Reanudar':'Pausar'}</button><select value={state.speed} onChange={(e)=>onSpeed(Number(e.target.value) as 1|2|4)}><option value={1}>x1</option><option value={2}>x2</option><option value={4}>x4</option></select><button onClick={onSave}>Guardar</button><button onClick={onLoad}>Cargar</button><button onClick={onExport}>Exportar JSON</button><label className="file-label">Importar<input type="file" accept="application/json" onChange={(e)=>{ const f=e.target.files?.[0]; if(f) onImport(f) }} /></label><button className="danger" onClick={onReset}>Reiniciar</button></header>
}
